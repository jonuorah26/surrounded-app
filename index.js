import { registerRootComponent } from "expo";
import Index from "./App";

// This ensures the environment is set up correctly, whether running in Expo Go or a bare build.
registerRootComponent(Index);
